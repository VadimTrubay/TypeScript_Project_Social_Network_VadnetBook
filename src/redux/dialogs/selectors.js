export const selectDialogs = (state) => state.dialogs.items || [];
export const selectIdActiveChat = (state) => state.dialogs.idActiveChat;

// export const selectRefresh = (state) => state.users.refreshed;
// export const selectLoadingUsers = (state) => state.users.loading;
